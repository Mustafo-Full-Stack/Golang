Первое задание: добавь в пакет pass
функцию CheckPassword, 
которая принимает строку и 
возвращает true, если пароль 
равен "go123". Вызови её из 
main.go и выведи результат.

Задание: добавь в пакет pass функцию
IsStrong, которая принимает пароль 
и возвращает true, если его длина
не меньше 8 символов. В main.go 
проверь с ней два пароля и выведи 
оба результата.

Добавь в User/User.go функцию
IsEven(number int) bool. 
Она должна возвращать true,
если число чётное, и false,
если нечётное. Подсказка: 
остаток от деления берётся 
через %. Например, 8 % 2 равно 0.

Следующее упражнение: добавь метод 
HaveBirthday, который увеличивает
возраст пользователя на 1. Используй
указатель, чтобы возраст изменился
у исходного пользователя.

В users/users.go добавь метод, который
отвечает на вопрос:
«пользователь совершеннолетний?» Он
должен вернуть true, если возраст 
18 или больше, иначе — false. 
Придумай методу понятное имя сам.


 -----НОВЫЙ------

Создай папку users и файл Go внутри
неё.Объяви там структуру пользователя
с именем и email.Напиши функцию, 
которая создаёт пользователя. 
Если имя пустое — сообщи об ошибке; 
иначе верни пользователя.
В main.go вызови эту функцию и 
выведи пользователя или ошибку.

Следующий шаг — научиться не 
создавать пользователя с пустым 
именем. Добавь функцию проверки 
в пакет users: если имя пустое, 
она должна сообщить об ошибке; 
иначе вернуть созданного пользователя.
Вспомни: чтобы функция могла вернуть 
и пользователя, и ошибку, в Go 
указывают два результата. Попробуй 
изменить GetUser, а я проверю.