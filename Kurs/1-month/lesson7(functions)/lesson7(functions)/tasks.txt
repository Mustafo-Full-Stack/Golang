**Задача 1.** Функция `isEven(n int) bool` — определить, чётное ли число n.

**Задача 2.** Функция `factorial(n int) int` — вычислить факториал числа n (через цикл, без рекурсии).

**Задача 3.** Функция `minMax(nums []int) (int, int)` — вернуть минимум и максимум среза (множественный возврат).

**Задача 4.** Функция `average(nums ...float64) float64` — среднее любого количества чисел (вариативная функция).

**Задача 5.** Функция `isPrime(n int) bool` — простое ли число n.

**Задача 6.** Функция `reverse(s string) string` — перевернуть строку.

**Задача 7.** Функция `countVowels(s string) int` — посчитать гласные буквы в строке.

**Задача 8.** Функция `circleAreaPerimeter(r float64) (area, perimeter float64)` — вернуть площадь и периметр круга именованными результатами.
