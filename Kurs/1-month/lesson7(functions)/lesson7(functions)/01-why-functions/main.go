package main

import (
	"fmt"

	examplefunc "why-functions/example-func"
)

// функция - это "кухонный комбайн": кладёшь внутрь продукты (аргументы),
// нажимаешь кнопку (вызов) - получаешь результат (возврат).
// не важно, что происходит внутри - важно, что на входе и что на выходе.

// вот проблема БЕЗ функций: одна и та же формула площади написана трижды.
// если формулу нужно поменять (например, добавить коэффициент) -
// придётся переписывать её в каждом месте и легко где-то забыть.
// func main() {
// 	a1 := 3 * 4
// 	a2 := 5 * 6
// 	a3 := 2 * 8
// 	fmt.Println(a1, a2, a3)
// }

/*

func имя(параметр1 Тип1, параметр2 Тип2) ТипРезультата {
	// тело
	return значение
}
*/

// решение: выносим формулу в отдельную функцию

func main() {
	// result := area(3, 6)
	// fmt.Printf("результат от функции area:%v", result)

	//fmt.Println(area(3, 5))
	//fmt.Println(area(3, 4), area(5, 6), area(2, 8))

	// теперь если формулу нужно поменять - правим только внутри area,
	// а не в трёх разных местах main

	// та же самая функция, но вынесенная в отдельный пакет example-func
	respones := examplefunc.PublicArea(3, 10)
	fmt.Println(respones)
}

func area(w, h int) int {
	return w * h
}

func perimeter(legth, witdth int) int {
	return 2 * (legth + witdth)
}
